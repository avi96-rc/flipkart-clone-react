import React from 'react';
import styles from './Main.module.css';

function Main(props) {
    return (
        <main className={styles.main}>
            
        </main>
    );
}

export default Main;